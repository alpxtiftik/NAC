client localhost {
    ipaddr          = 127.0.0.1
    secret          = testing123
    shortname       = localhost
}

client docker-network {
    ipaddr          = 172.0.0.0/8
    secret          = testing123
    shortname       = docker
}